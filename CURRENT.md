# 当前状态

## 阶段

技术文档精读助手 `v0.2` 的 P1–P5 与本地完成验收均已结束。本次“产品可在本地正常执行”目标已完成；P6 上线和 P7 面试材料未开始，也不在本次目标。

## 已确认

- 工作目录：`/Users/Admin/Developer/AI/sainuo-meet-work`
- 原始题目：`AI 产品应用开发面试作业.pdf`
- Git 已初始化，尚未创建首次提交。
- 目标用户：需要精读陌生技术原理或规范的软件开发者。
- 主要材料：RFC、协议规范、架构设计和原理说明类长文档。
- 核心闭环：导入文档 → 设置阅读画像 → 文档地图与路线 → 阶段精读 → 引用定位 → 编辑并确认笔记 → Markdown 导出。
- 输入支持文件上传与用户指定公开 URL；不主动联网搜索。
- 没有文档时不提供通用聊天。
- 默认中文解释，保留英文术语、规范关键词和代码原文。
- 模型基线为用户提供的 OpenAI-compatible Qwen API，默认 `qwen3.6-flash`；首版使用本地章节/关键词检索。
- 前期开发与交付材料优先使用 RFC 6455 或获准使用的 MyBatis 事务规范。
- 文档按用途和规模分类；大型教程先选章节/主题，截图型文档显示解析质量告警。
- 数据采用匿名临时会话，默认 2 小时 TTL。
- 目标交付为 1–2 个开发日的公开 Web Demo，随后制作 5–10 页 PPT/PDF。
- P1 已验证 5 份本地材料与 RFC 6455：MyBatis/RFC 检索基准 10/10 通过，`qwen3.6-flash` 的结构化 JSON、SSE 和图片输入协议探针通过。
- 视觉探针不等于 OCR/图表理解；截图型 PDF 仍属于显式降级场景。
- P2–P5 已实现匿名会话、四类文件与受控 URL、文档地图、3–6 阶段路线、可信来源抽屉、SSE 精读、确认式笔记和 Markdown 下载。
- 真实 MyBatis 浏览器主流程通过：57 页、80 个来源块、4 阶段路线、首阶段讲解与引用、草稿编辑接受、下载和刷新恢复。
- 390 px 移动验收无横向溢出；桌面/移动控制台均无 warning/error。
- 最新工程验证为 16 个测试文件、59 项测试，TypeScript、生产构建和依赖审计通过，0 个已知漏洞。

## 外部依赖

- 用户已提供允许测试的混合格式资料，并确认 MyBatis 规范允许用于本项目。
- 用户已提供 Qwen API 配置和开发期测试权限；公开部署前仍需确认费用上限。
- P6 开始前确认托管平台、公开期限和访问保护。
- P7 开始前确认最终材料优先使用 PPTX 还是 PDF。
- 当前开发网络把 RFC 域名解析到保留测试网段；生产 URL 网关的允许地址端到端验证顺延到普通网络或 P6 部署环境。

## 当前边界

- 旧课程学习助手方案已被删除，不再作为实现依据。
- 本地验收证据见 `docs/reports/LOCAL_ACCEPTANCE.md`；阶段事实见 P1–P5 报告。
- 用户体验反馈单独维护在 `docs/UX_ISSUES.md`，不与 `docs/OPEN_ISSUES.md` 的技术、模型和部署待决项混合。
- 首轮体验修正已处理文件选择后的流程提示和 Next.js `N` 浮标；完整体验审查见 `docs/reports/UX_AND_AI_TUTOR_REVIEW.md`。
- 当前体验优化优先级锁定为 Markdown 完成后统一渲染、精读助手侧边抽屉、主流程复验，再进入 P6 公网部署。
- AI 助教暂定为 `v0.3 / P6-AI 助教实验`：单文档、按需检索原文/地图/已确认笔记，不联网、不跨文档、不做长期记忆。
- 本地会话采用进程内存储，服务重启后允许丢失；公开上线前必须重新评估持久化和多实例边界。
- 引用 ID 已防伪，但合法 ID 不自动证明模型每句话都被片段语义支持；需继续做 RFC 和开放式问题回归。
- 后续工作继续以 `docs/PRD.md`、`docs/ARCHITECTURE.md` 和 `docs/PLANNING.md` 为准。

## 下一步

1. 用户按 `docs/reports/LOCAL_ACCEPTANCE.md` 审阅本地产品闭环和残余边界。
2. 轮换曾在截图和 CSV 中暴露的 API key，并重新运行 `npm run config:import -- /absolute/path/to/new-config.csv`。
3. 如继续 P6，先确认托管平台、公开期限、访问保护、模型预算和 URL 允许路径测试网络。
4. 先处理 UX-007 至 UX-011：Markdown 消息层、侧边抽屉、阶段状态和引用交互；完成后请用户复验。
5. 按报告顺序进入 P6 公网质量与上线；AI 助教仅在主 Demo 稳定后作为 v0.3 实验。
