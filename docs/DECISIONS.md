# 实施决策记录

本文件记录 P2–P5 开发期间由实现证据触发、且前期未完全锁定的默认决策。决策如需修改，先更新本文件和对应阶段报告，再调整实现。

| ID | 日期 | 阶段 | 决策 | 理由与影响 | 状态 |
| --- | --- | --- | --- | --- | --- |
| D-001 | 2026-09-02 | P2 | 超过 500,000 字符的文档允许完成解析和规模判断，但索引块按顺序截断到上限并显示告警 | 316 页回归材料实测为 576,976 字符；直接拒绝会与“大书先选范围”的产品目标冲突 | 已采用 |
| D-002 | 2026-09-02 | P2 | 当前开发网络中的 `198.18.0.0/15` 映射不加入 URL 安全白名单 | 放宽会破坏 SSRF 边界；本地完整 Demo 改用文件上传，URL 端到端允许地址在普通或部署网络补测 | 已采用 |
| D-003 | 2026-09-02 | P2 | 匿名会话使用 HttpOnly Cookie + 进程内 TTL Store，刷新可恢复，服务重启允许丢失 | 与 v0.2 单实例、本地可运行边界一致，不提前引入数据库 | 已采用 |
| D-004 | 2026-09-02 | P2 | Markdown 单文件上传不读取其相邻目录资源 | 浏览器只上传一个文件，服务端不能安全推断或读取客户端目录；相对图片作为缺失资源告警 | 已采用 |
| D-005 | 2026-09-02 | P3 | 路线中的 `sourceScopes` 只能从服务端目录/页段候选中选择 | 防止模型编造章节或页码；不匹配时修复一次，失败则显式重试 | 已采用 |
| D-006 | 2026-09-02 | P3 | 结构摘要采用目录 + 均匀代表片段，不把 500,000 字符索引全文发给模型 | 控制延迟、费用和数据暴露，仍覆盖文档开头/中部/结尾 | 已采用 |
| D-007 | 2026-09-02 | P3 | 模型使用 `SC01` 等稳定范围编号，服务端再映射回真实标题 | 真实调用证明直接要求逐字复述长目录字符串会连续校验失败；编号保持白名单约束且降低输出脆弱性 | 已采用 |
| D-008 | 2026-09-02 | P3 | 文档地图和受来源约束的讲解默认关闭 Qwen thinking，可用环境变量恢复 | 首次有效路线单次调用耗时 59 秒，P1/P3 均显示大量 reasoning tokens；该任务已有结构和证据约束，优先满足交互延迟 | 已采用，需持续评估质量 |
| D-009 | 2026-09-02 | P4 | 理解检查问题由服务端基于阶段目标生成，不增加独立模型调用 | 保证每阶段必有检查问题，降低成本和失败面；模型仍负责对用户答案给反馈 | 已采用 |
| D-010 | 2026-09-02 | P4 | 流式 token 只作为临时 UI 状态，完整结束后才写入会话 | 避免中断导致阶段、消息和笔记状态不一致 | 已采用 |
| D-011 | 2026-09-02 | P5 | 模型返回笔记要点 JSON，服务端渲染固定 Markdown | 稳定导出结构，让模型专注内容，让用户编辑最终文本 | 已采用 |
| D-012 | 2026-09-02 | P5 | 已决议草稿的相同动作重试幂等，冲突动作返回 409 | 防止网络重试重复写入，也不允许悄悄反转用户决定 | 已采用 |
| D-013 | 2026-09-02 | P5 | 导出只读取 accepted 草稿，并中和危险 HTML/链接 | 明确成果所有权并降低下载后渲染风险 | 已采用 |
| D-014 | 2026-09-02 | 本地验收 | 浏览器同源校验使用请求 `Host` 与协议重建外部原点，不依赖 Next.js 规范化后的 `nextUrl.origin` | `127.0.0.1` 在本地桥接环境被误判为非 `localhost`；新规则仍拒绝跨主机 Origin | 已采用 |
| D-015 | 2026-09-02 | 本地验收 | 刷新后的阶段优先级为 active/awaiting_note → pending → 最后一个 completed | 避免完成首阶段后刷新又回到已完成内容，保持阅读连续性 | 已采用 |
| D-016 | 2026-09-02 | 本地验收 | 组织约束文档同时要求“该文档规定”措辞和不得补充来源片段外的文档事实 | 真实讲解曾把通用 Spring 背景与合法引用混用；提示收紧但仍需语义支持度评测 | 已采用，需持续评估 |
| D-017 | 2026-09-02 | P5.5 | 首页、阅读画像、路线工作区分别使用 `/home`、`/profile`、`/reading`，路由切换不触发模型调用 | 浏览器历史需要表达用户访问位置；进入画像不等于确认重生成，只有提交表单才花费 token 和清空进度 | 已采用 |
| D-018 | 2026-09-02 | P5.5 | 左侧栏按“工作区导航”设计：来源标题常驻，详情悬浮；新建会话与收起可用，历史/设置/登录先提供明确占位 | 减少来源元数据长期占位，同时建立完整但不伪造未实现能力的产品骨架 | 已采用 |
| D-019 | 2026-09-02 | P5.5 | `v0.2` 保留单活动内存会话，不实现历史列表、数据库或登录；领域文案使用“新建精读” | 历史需要主体所有权和持久化，数据库加登录会把面试 Demo 扩张为多用户 SaaS；新建能力仅用于重新演示主流程 | 已决定，待界面同步 |
| D-020 | 2026-09-02 | P5.5/P6 | Next.js 开发工具继续隐藏；如需诊断，使用生产默认关闭的自有中文面板 | 框架工具不属于产品 UI，不能可靠本地化，也不应向评审暴露内部配置 | 已决定 |
