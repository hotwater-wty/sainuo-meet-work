# Lumen 交付与部署说明

本说明面向评审者与运维方，介绍 Lumen（技术文档精读助手）的**首版产品边界、部署与上线方案、部署前提，以及验证与质量结果**。产品功能介绍与体验方式见根目录 [`README.md`](./README.md)；完整设计基线、验收报告与归档文档见 [`docs/README.md`](./docs/README.md)。

## 首版边界

- 没有文档时不提供通用聊天，不主动搜索网页。
- 不支持 OCR、图片 / 图表理解、公式结构恢复。
- 首个版本只处理单文档、匿名临时会话，不扩展为知识库或 Agent 平台。
- 仅使用用户提供的 OpenAI-compatible Qwen API（默认 `qwen3.6-flash`）。

## 部署与上线

当前仓库为 **v0.2 本地交付版**：P1–P5.5 已完成并通过本地验收，P6 公网部署尚未开始。

- 采用单实例长驻 Node 容器运行，`/api/health` 只返回存活状态与版本，不暴露供应商或密钥信息。
- 会话为匿名临时会话，保存在进程内内存（最后活动 2 小时 TTL），服务重启或多实例切换会丢失。
- 公网上线前需完成下方「部署前提」的确认，并建议将会话存储迁移到带 TTL 的外部存储。

## 部署前提（P6 前需确认）

| 事项 | 说明 |
| --- | --- |
| 会话存储 | 当前为单进程内存 TTL，服务重启或多实例切换会丢失会话。公网上线前建议迁移到带 TTL 的外部存储（已完成方案评估，见 `docs/archive/REDIS_SESSION_STORE_FEASIBILITY.md`）。 |
| 模型预算 | 确认 Qwen 模型单价与日预算，设置限流、日总量上限与必要的访问口令。 |
| 托管平台 | 确认部署平台、运行期限、公开访问保护与下线方式。 |
| URL 网络 | 在普通本机网络或部署环境补测公开 URL 的生产网关允许路径。 |

## 技术栈

| 层面 | 技术 | 用途 |
| --- | --- | --- |
| 应用框架 | Next.js 16 (App Router) + React 19 | 页面、API 与工作区界面 |
| 语言 | TypeScript | 类型安全与上下文隔离 |
| 文档解析 | `pdfjs-dist` | PDF 按页提取文本并保留页码 |
| 本地检索 | `MiniSearch` | 会话级倒排索引、章节/关键词/标识符匹配 |
| 模型 | OpenAI-compatible Qwen API（默认 `qwen3.6-flash`） | 文档地图、路线、阶段讲解与笔记草稿 |
| 数据校验 | Zod | 校验客户端输入、内部模型与模型结构化输出 |
| 部署形态 | 单实例长驻 Node 容器 | 匿名内存会话与 TTL 的生命周期匹配 |

## 验证与质量

```bash
npm run typecheck
npm test
npm run build
npm audit --omit=dev
```

最新完整结果：17 个测试文件、64 项测试通过，生产构建通过，依赖审计为 0 个已知漏洞；390 px 移动视口无横向溢出。详见 `docs/reports/LOCAL_ACCEPTANCE.md`。
