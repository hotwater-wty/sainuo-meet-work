# 项目协作约定

## 项目目标

本仓库用于完成公司提供的 AI 产品应用开发面试作业。开发按 `docs/PLANNING.md` 的滚动阶段推进，每阶段验证和交接后再进入下一阶段。

## 信息来源与优先级

1. 用户在当前对话中的明确要求优先。
2. `AI 产品应用开发面试作业.pdf` 是场景和交付要求的原始材料；在后续调研阶段提取并核对其内容。
3. 已确认的需求、决策、风险、环境信息和下一步维护在 `CURRENT.md`。
4. 其他文档和代码仅在其适用范围内具有约束力。

## 工作方式

- 开始实际开发前，先阅读题目 PDF，输出需求拆解、假设、待确认项和可验证的验收标准。
- 先选择与题目约束相匹配的技术栈和部署目标，再创建应用代码及基础设施配置；不提前假定框架、云服务或密钥方案。
- 每项实现应包含适度的验证：静态检查、自动化测试、构建，或可复现的手工验收步骤。
- 修改 `CURRENT.md` 记录会影响后续工作的已确认决策、阻塞项和下一步；不要把临时推测写成事实。
- 保持提交小而聚焦，避免无关重构，不覆盖或回退未确认的既有改动。
- 开始任何开发阶段前，阅读 `docs/PRD.md`、`docs/ARCHITECTURE.md` 和 `docs/PLANNING.md`，确认当前阶段及退出标准。
- `docs/PLANNING.md` 是唯一滚动进度入口；同一时间只能有一个阶段处于进行中。
- 范围、数据、安全、成本或接口发生变化时，先更新设计基线与 `CURRENT.md`，再实施代码变更。

## 阶段交接

- 开发按可独立验收的阶段推进；开始下一阶段前，先完成当前阶段的验证和交接说明。
- 每个阶段结束后，必须向用户说明：本阶段解决的业务问题、确定的业务约定、核心数据流、关键技术取舍、已知限制、验证结果和下一阶段依赖。
- 讲解以产品行为和系统边界为主，不把语法、逐行代码或框架术语作为主要内容；必要术语应先用业务语言解释。
- 对模型、检索、存储、权限、外部工具和部署等可能失控的部分，明确说明控制点、失败时的表现和回退方式。
- 阶段交接后接受用户审阅和调整；影响范围、数据、安全或成本的新增决定，应先更新设计文档再实施。
- 阶段只有在交付物、验证结果、文档同步和用户交接全部完成后才能标记为已完成。

## 安全与配置

- 不提交密钥、令牌、真实生产数据、私有证书或本机环境文件。
- 新增环境变量时，同时维护 `.env.example`，只提供无敏感信息的占位值和用途说明。
- 任何涉及外部发布、付费资源、生产数据或第三方账号的操作，先确认目标和授权范围。

## 文档约定

- `README.md`：面向使用者与评审者的项目说明、启动方式和验证入口。
- `CURRENT.md`：当前进度、已知事实、待确认项和下一步。
- `docs/`：开发前的需求、可行性、技术选型和架构讨论稿。
- `docs/PLANNING.md`：开发阶段、验收门槛、需求追踪和决策记录。
- 本文件：持续有效的协作和工程约定。
